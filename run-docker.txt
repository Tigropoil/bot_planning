CREER L'IMAGE

docker build -t bot-planning .


BASH

docker run -d --name bot-planning \
  --env-file .\.env \
  --restart always \
  bot-planning:latest


POWERSHELL

docker run -d --name bot-planning `
  --env-file .\.env `
  --restart always `
  bot-planning:latest
